// MaritimeHeader.js
import React from "react";
import HeaderNav from "./HeaderNav"; // Import the reusable HeaderNav component

const MaritimeHeader = () => {
  // Use the HeaderNav component and pass "MARITIME" as the title
  return <HeaderNav title="MARITIME" />;
};

export default MaritimeHeader;
