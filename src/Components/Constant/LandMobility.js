// LandMobilityHeaderTwo.js
import React from "react";
import HeaderNav from "./HeaderNav"; // Import the reusable HeaderNav component

const LandMobilityHeaderTwo = () => {
  // Use the HeaderNav component and pass "LAND MOBILITY" as the title
  return <HeaderNav title="LAND MOBILITY" />;
};

export default LandMobilityHeaderTwo;
